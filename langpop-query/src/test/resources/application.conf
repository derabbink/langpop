test {
	langpop {
		query {
			
		}
	}
}

akka {
	event-handlers = ["akka.event.slf4j.Slf4jEventHandler"]
	loglevel = DEBUG
	log-config-on-start = off
	actor.debug {
		receive = on
		autoreceive = on
		lifecycle = on
		event-stream = on
	}
}
