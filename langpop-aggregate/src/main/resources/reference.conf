langpop-aggregate {
	akka {
	event-handlers = ["akka.event.slf4j.Slf4jEventHandler"]
	loglevel = DEBUG
		log-config-on-start = on
		actor.debug {
			receive = on
			autoreceive = on
			lifecycle = on
			event-stream = on
		}
		
	}
}
